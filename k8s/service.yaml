apiVersion: v1
kind: Service
metadata:
  name: heart-api-service
spec:
  type: LoadBalancer
  selector:
    app: heart-api
  ports:
    - protocol: TCP
      port: 80         # Port exposed to the outside
      targetPort: 8000 # Port your FastAPI app runs on inside the container